# python
__pycache__/
*.py[cod]
.venv/
.uv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# env & secrets
.env
.env.local
*.pem
*.key

# local-machine compose overrides (e.g. port remaps around a port conflict)
docker-compose.override.yml

# data & artifacts
data/generated/
data/reports/
ml/mlruns/
*.joblib
*.lgb
voice/out/
reports/

# ml/artifacts/*.png and metrics.json ARE committed — 04-EXECUTION-PLAN.md's stack
# rationale is explicit that artifacts a judge can read in the repo beat a server
# they can't see, and the phase-03 DoD names the confusion matrix and calibration
# plot specifically. Only the large, regenerable binaries above are excluded.

# node
node_modules/
web/.next/
web/out/
web/tsconfig.tsbuildinfo
.turbo/

# os / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# transient
.wt
*.tar.gz
